<%= form_for [ @article, @comment ] do |f| %>
  <p>
    <%= f.label "Comment as #{@article.user.profile.username}" %>
    <%= f.hidden_field 'user_id', value: current_user.id %>
  </p>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>